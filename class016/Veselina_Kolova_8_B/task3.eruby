Develop a program named FirstName_LastName_ClassNumber_<%=@task_number%>.rb

1. you are given two arguments for a folders with files;
1.1 if there are other arguments they should be discarded;
2. Find <%= @what_to_find %>;
3. Calculate <%= @what_to_calculate %>;
4. Sort the result by <%= @which_to_sort %> <%= @in_what_order %>;
5. Produce a result in <%= @file_format %> format from <%= @file_format %>_writer_ClassNumber.rb named result.<%= @file_format %>:

<%= @to_expect %>
