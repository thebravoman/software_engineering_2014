- you are given an argument for a folder with .rb files
- produce a script that gets the filenames inside that folder with these conditions:
	= the file must be in format Firstname_Lastname_Number.rb
	= the file must have a Number less than 10 -------^
- sort the results ASC by Number ---------------------^
- save the results with the given JSON writer with .write(ARRAY)
	= ARRAY holds the results (each element is a filename)


