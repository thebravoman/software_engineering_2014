Develop a program named FirstName_LastName_ClassNumber_<%=@task_number%>.rb

1. Find the sum of 1 to N
1.1 N == ARGV[0]
2. Save the result in XML file format in <sum></sum> tag (result.xml).
3. Save the result in HTML file format in <%=@tag%> tag (result.html).
4. Use two different classes to save the result !
	Example:
		XML:
			<sum>15</sum>
		HTML:
			<%=@exmpl%>
